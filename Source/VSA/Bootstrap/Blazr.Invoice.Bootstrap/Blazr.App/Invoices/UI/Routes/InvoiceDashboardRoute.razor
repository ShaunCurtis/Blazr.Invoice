@page "/invoice/{Id:guid}"
@page "/invoice/dash/{Id:guid}"

@namespace Blazr.App.UI

<InvoiceDashboard InvoiceId="_invoiceId" />

@code{
    [Parameter] public Guid Id { get; set; }

    private InvoiceId _invoiceId => new(Id);
}